name: Generar Gradle Wrapper y Dar Permisos
on: workflow_dispatch # Esto permite ejecutarlo manualmente

jobs:
  setup:
    runs-on: ubuntu-latest
    permissions:
      contents: write # Permiso para que la acción pueda subir archivos
    steps:
      - uses: actions/checkout@v4
      
      - name: Configurar Java
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'
          
      - name: Generar Gradle Wrapper
        run: gradle wrapper
        
      - name: Dar permisos de ejecución a gradlew
        run: chmod +x ./gradlew
        
      - name: Subir archivos al repositorio
        run: |
          git config --global user.name "github-actions[bot]"
          git config --global user.email "github-actions[bot]@users.noreply.github.com"
          git add gradlew gradlew.bat gradle/
          git commit -m "Añadido Gradle Wrapper y permisos automáticamente"
          git push
          
